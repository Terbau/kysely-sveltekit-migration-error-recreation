import { db } from '$lib/server/db';

// (async () => {
//     const person = await db
//         .selectFrom('user')
//         .executeTakeFirst();

//     console.log("This should be null:", person);
// })();